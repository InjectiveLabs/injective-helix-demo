<script setup lang="ts">
//
</script>

<template>
  <div class="table-label flex p-2">
    <div class="flex-1 p-2">{{ $t('trade.lastUpdated') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.market') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.type') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.side') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.price') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.amount') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.total') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.triggerCondition') }}</div>
    <div class="flex-1 p-2">{{ $t('trade.status') }}</div>
  </div>
</template>

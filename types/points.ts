export type AccountPoints = {
  rank: string
  totalPoints: string
  updatedAt: string
}

export type HistoricalPoints = {
  points: string
  volume: number
  periodStart: string
  periodEnd: string
}

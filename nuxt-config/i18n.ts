import en from '@/locales/en'

export default {
  i18n: {
    locales: ['en'],
    vueI18n: {
      locale: 'en',
      fallbackLocale: 'en',
      availableLocales: ['en'],
      messages: { en }
    }
  }
}

import en from './../locales/en'

export default {
  localeDir: 'locales',
  vueI18n: {
    globalInjection: true,
    legacy: false,
    locale: 'en',
    fallbackLocale: 'en',
    availableLocales: ['en'],
    messages: { en }
  }
}
